{
  "$schema": "https://schema.tauri.app/capability/2",
  "identifier": "remote-ui-notifications",
  "description": "Allow the BixDot UI (served by the local backend over http://localhost:8747) to send native OS notifications. This is the ONLY IPC surface exposed to the webview.",
  "windows": ["main"],
  "remote": {
    "urls": ["http://localhost:8747"]
  },
  "permissions": [
    "core:default",
    "notification:default"
  ]
}
