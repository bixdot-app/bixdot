# BixDot — Production Dependencies
# Python 3.11+
# Install: pip install -r requirements.txt
# For development/CI tools: pip install -r requirements-dev.txt

# Web framework
# Upper bounds on fastapi/starlette are deliberate (BXD-020): a past release
# wrapped every app.include_router(...) call in an internal nested-router
# object instead of flattening routes into app.routes, which silently broke
# tests/test_route_auth.py's route enumeration down to 3 checked routes out
# of ~70+. tests/test_route_auth.py::_api_routes() now walks the nested
# object's .original_router.routes and is verified against this pinned
# range. Bumping either ceiling requires re-running that verification
# (import core.main; len(_api_routes()) should stay in the 80s, not drop to
# single digits) before widening the pin.
fastapi>=0.116.0,<0.142.0
uvicorn[standard]>=0.30.6

# Authentication
PyJWT>=2.13.0
bcrypt>=4.2.0
cryptography>=46.0.7

# Storage
keyring>=25.4.1         # OS keyring for secrets
aiofiles>=24.1.0        # Serve static frontend files

# LLM
anthropic>=0.34.2       # Claude API (optional cloud mode)
httpx>=0.28.1           # Async HTTP

# Security
python-multipart>=0.0.27
slowapi>=0.1.9          # Rate limiting

# Validation
pydantic>=2.9.2
pydantic-settings>=2.5.2

# Utilities
python-dotenv>=1.2.2
structlog>=24.4.0       # Structured logging

# Skills
ddgs>=9.0.0             # MIT — web search, no API key needed
icalendar>=5.0.0        # BSD-2-Clause — .ics calendar file support
markitdown[pdf,docx,pptx,xlsx]>=0.1.6  # MIT (Microsoft) — PDF, DOCX, PPTX, XLSX parsing
trafilatura>=2.0.0      # Apache 2.0 — web page text extraction for deep research
numpy>=1.26.0           # BSD-3 — cosine similarity for Ask My Files (local embeddings)
psutil>=6.0.0           # BSD-3 — RAM/disk check for model recommendations

# Security pins — transitive dep minimums enforced to avoid known CVEs
starlette>=0.47.2,<1.7.0  # CVE-2024-47874, CVE-2025-54121, PYSEC-2026-161; ceiling: see BXD-020 note above
urllib3>=2.6.3         # CVE-2025-66418, CVE-2025-66471, CVE-2026-21441
requests>=2.33.0       # CVE-2026-25645
jinja2>=3.1.6          # CVE-2024-56326, CVE-2024-56201, CVE-2025-27516
idna>=3.15             # CVE-2026-45409
filelock>=3.20.3       # CVE-2025-68146, CVE-2026-22701
pillow>=12.2.0         # CVE-2026-25990, CVE-2026-40192, CVE-2026-42310, PYSEC-2026-165
