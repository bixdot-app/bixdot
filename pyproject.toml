[build-system]
requires = ["setuptools>=68", "wheel"]
build-backend = "setuptools.backends.legacy:build"

[project]
name = "bixdot"
version = "0.6.3"
description = "The secure, local-first AI agent. No cloud required."
readme = "README.md"
license = { file = "LICENSE" }
authors = [
    { name = "DigiTech Business Pte. Ltd", email = "hello@bixdot.app" }
]
keywords = ["ai-agent", "security", "local-first", "zero-trust", "llm", "ollama"]
classifiers = [
    "Development Status :: 3 - Alpha",
    "Intended Audience :: Developers",
    "Operating System :: OS Independent",
    "Programming Language :: Python :: 3.11",
    "Programming Language :: Python :: 3.12",
    "Topic :: Security",
    "Topic :: Scientific/Engineering :: Artificial Intelligence",
]
requires-python = ">=3.11"
dependencies = [
    "fastapi>=0.116.0",
    "uvicorn[standard]>=0.30.6",
    "PyJWT>=2.13.0",
    "bcrypt>=4.2.0",
    "cryptography>=46.0.7",
    "keyring>=25.4.1",
    "anthropic>=0.34.2",
    "httpx>=0.28.1",
    "pydantic>=2.9.2",
    "pydantic-settings>=2.5.2",
    "python-dotenv>=1.2.2",
    "structlog>=24.4.0",
    "slowapi>=0.1.9",
    "python-multipart>=0.0.27",
]

[project.optional-dependencies]
dev = [
    "pytest>=8.3.3",
    "pytest-asyncio>=0.24.0",
    "bandit>=1.7.10",
    "ruff>=0.6.0",
    "pip-audit>=2.7.0",
]

[project.urls]
Homepage = "https://bixdot.app"
Repository = "https://github.com/bixdot-app/bixdot"
"Bug Tracker" = "https://github.com/bixdot-app/bixdot/issues"
Security = "https://github.com/bixdot-app/bixdot/security/policy"
Changelog = "https://github.com/bixdot-app/bixdot/blob/main/CHANGELOG.md"

[project.scripts]
bixdot = "core.main:app"

[tool.ruff]
line-length = 100
target-version = "py311"

[tool.ruff.lint]
select = ["E", "W", "F"]
ignore = ["E501"]

[tool.pytest.ini_options]
asyncio_mode = "auto"
testpaths = ["tests"]
